#include "main.h"

/**
 * swap_int - swaps the values of integers
 * @a: first parameter
 * @b: second parameter
 * Return: no return
 */
void swap_int(int *a, int *b)
{
	int n = *a;
	int *a = int *b;
	int *b = int n;
}
